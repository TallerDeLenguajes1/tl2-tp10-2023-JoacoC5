@model ViewUsuarioUpdate

<form asp-controller="Usuario" asp-action="ModificarUsuario" method="post">
    <div class="text-center">
        <div class="mb-3">
            <label asp-for="@Model.Id" class="form-group">Id</label>
            <input asp-for="@Model.Id" value="@Model.Id" readonly>
        </div>
        <div class="mb-3">
            <label asp-for="@Model.NombreDeUsuario" class="form-group">Nombre de usuario</label>
            <input asp-for="@Model.NombreDeUsuario" required maxlength="25">
        </div>
        <div class="mb-3">
            <label asp-for="@Model.Contrasenia" class="form-group">Contraseña</label>
            <input type="password" asp-for="@Model.Contrasenia" required>
        </div>
        <div class="mb-3">
            <label asp-for="@Model.Rol" class="form-group">Rol</label>
            <input asp-for="@Model.Rol" value="@Model.Rol" readonly>
        </div>

        <div class="mb-3">
            <button type="submit" asp-route-idBuscado="@Model.Id" class="btn btn-success">Enviar</button>
            <button><a asp-controller="Usuario" asp-action="ListarUsuario" class="link-secondary">Cancelar</a></button>
        </div>
    </div>
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}