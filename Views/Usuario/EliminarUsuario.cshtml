@model Usuario

<form asp-controller="Usuario" asp-action="EliminarUsuario">
    <div class="text-center">
        <div class="mb-3">
            <label asp-for="@Model.Id" class="form-group">Id</label>
            <input asp-for="@Model.Id" value="@Model.Id" readonly>
        </div>
        <div class="mb-3">
            <label asp-for="@Model.NombreDeUsuario" class="form-group">Nombre de usuario</label>
            <input asp-for="@Model.NombreDeUsuario" value="@Model.NombreDeUsuario" readonly>
        </div>
        <div class="mb-3">
            <button type="submit" onclick="return confirm('¿Quiere eliminar el elemento?')" class="link-danger">Eliminar
                usuario</button>
            <button><a asp-controller="Usuario" asp-action="ListarUsuario" class="link-secondary">Cancelar</a></button>
        </div>
    </div>
</form>