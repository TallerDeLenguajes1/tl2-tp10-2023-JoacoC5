@model ViewTablero

<form asp-controller="Tablero" asp-action="EliminarTablero">
    <div class="text-center">
        <div class="mb-3">
            @*<label asp-for="@Model.Id" class="form-group">Id</label>*@
            <input asp-for="@Model.Id" value="@Model.Id" hidden>
        </div>
        <div class="mb-3">
            <label asp-for="@Model.Nombre" class="form-group">Nombre</label>
            <input asp-for="@Model.Nombre" value="@Model.Nombre" readonly>
        </div>
        <div class="mb-3">
            <button type="submit" onclick="return confirm('¿Quiere eliminar el elemento?')" class="link-danger">Eliminar
                tablero</button>
            <button><a asp-controller="Tablero" asp-action="ListarTablero" class="link-secondary">Cancelar</a></button>
        </div>
    </div>

</form>
